# Copy to .env and adjust for local/AWS runs. Nothing here is required to
# run the project locally with default config.yaml Values.

# Override the config.yaml path if needed
# PURCHASE_INTENT_CONFIG_PATH=src/config/config.yaml

# API
API_HOST=0.0.0.0
API_PORT=8000

# Dashboard
DASHBOARD_PORT=8501

# AWS (only needed for the stretch-goal cloud deployment; not required to pass core requirements)
# AWS_REGION=us-east-1
# ECR_REPOSITORY_API=purchase-intent-xai-api
# ECR_REPOSITORY_DASHBOARD=purchase-intent-xai-dashboard
